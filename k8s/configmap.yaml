apiVersion: v1
kind: ConfigMap
metadata:
  name: authwarden-config
  namespace: authwarden
data:
  APP_HOST: "0.0.0.0"
  APP_PORT: "8080"
  TRUST_PROXY_HEADERS: "true"
  OAUTH_HTTP_TIMEOUT_SECONDS: "5"
